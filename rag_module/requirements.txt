# RAG 知识库依赖（导入 + 检索）。本地和云服务器部署前需安装：
#   pip install -r rag_module/requirements.txt
# 若报错找不到 llama-index>=0.10.0，多半是 pip 源太旧，请用官方源：
#   pip install -i https://pypi.org/simple/ -r rag_module/requirements.txt
# 或先升级 pip：python3 -m pip install --upgrade pip

# 解析、混合向量与重排。限制主版本，避免云端安装时 API 漂移。
llama-index-core>=0.14,<0.15
llama-index-readers-file>=0.5,<0.6
qdrant-client>=1.17,<2
FlagEmbedding>=1.4,<2
# FlagEmbedding 的 encoder-only reranker 仍调用 tokenizer.prepare_for_model；
# Transformers v5 调整了该接口，会让 bge-reranker-v2-m3 在重排时报错。
transformers>=4.44.2,<5

# 配置与文档解析
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
python-pptx>=0.6.0
pandas>=2.0.0
openpyxl>=3.0.0
pdfplumber>=0.10.0

# 可选：扫描版 PDF 用 OCR（云上如需再装）
# 系统需先安装：yum install -y tesseract tesseract-langpack-chi-sim poppler-utils
pdf2image
pytesseract
pillow
